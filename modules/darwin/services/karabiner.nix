{pkgs, ...}: {
  # Broken in latest releases

  # home-manager.users."eru".home.file.".config/karabiner" = {
  #   source = ./karabiner-config;
  #   recursive = true;
  # };
  #
  # services.karabiner-elements = {
  #   enable = true;
  # };
}
